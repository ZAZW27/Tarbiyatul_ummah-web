{
    "name": "tarbiyatul-ummah",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "src/frontend",
        "src/backend/api"
    ],
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier . --write",
        "format:check": "prettier . --check",
        "lint": "eslint .",
        "vercel-build": "prisma generate --schema=./src/backend/api/prisma/schema.prisma && prisma migrate deploy --schema=./src/backend/api/prisma/schema.prisma"
    },
    "author": "Zharif Aziz Zulkarnain Widodo",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^3.9.5",
        "prisma": "^7.9.1"
    }
}
